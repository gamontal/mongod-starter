language: objective-c

branches:
  only:
  - master

before_install:
  - brew update

xcode_project: mongod-starter.xcodeproj
xcode_scheme: mongod-starter
osx_image: xcode7.3
xcode_sdk: macosx10.11

script:
  xctool clean build test -project mongod-starter.xcodeproj -scheme mongod-starter

